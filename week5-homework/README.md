```text
/auth 
  POST /login - 미완성 (JWT로 바꿔주기)
  POST /signup
```

```text
/users
  GET /:id
  PATCH /:id
  DELETE /:id
```

```text
/posts - 관련된 데이터들 DB로 이주시키기
  POST /
  GET /
  GET /:id
  PATCH /:postId
  DELETE /:postId
```

```text
/comments
  - post에 아직 comment가 등록되는 상태 x
  - 관련된 데이터들 DB로 이주시키기
  POST /
  GET /
  GET /:id
  PATCH /:id
  DELETE /:id

```
